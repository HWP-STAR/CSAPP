# ./btest -f 函数名字
对单个函数进行测试

./btest #查看总分
# data lab
~（按位取反）和 |（按位或）这两个运算符

&：都是1才return 1,其他都是0
eg：&用 0xFF 把高 24 位清零

# 无论是十进制还是十六进制，它们只是同一种数值的不同书面表示形式，计算机内部存储的二进制位是完全相同的。
同一位数（如32位），表示都一样，没有用的用0站位
eg：在 32 位整数中，0xFF 实际是 0000 0000 0000 0000 0000 0000 1111 1111。

2位0x表示1字节（8bit），eg：0x11,0xff,都是1字节

# 操作
<< 相当于*2次方

# 逻辑右移功能，将 x 右移 n 位，高位补 0
>> 运算符执行的是算术右移：

正数：最高位补 0

负数：最高位补 1（保留符号位）

C 语言中：
有符号数（如 int）使用算术右移
无符号数（如 unsigned int）使用逻辑右移
# %010X 十六进制表示，10是10个格子大小，0是填充，没有就空格填充

# 在计算机中二进制本身没有正负之分。正负是人赋予它的解释。
eg：1000 0111 0110 0101 0100 0011 0010 0001
如果你把它当作 unsigned int（无符号整数），它就是正数 2271560481。
如果你把它当作 int（有符号整数），由于最高位是 1，它就被解释为负数 -2023406815。
同一个位模式，两种解读，两个不同的数值。
