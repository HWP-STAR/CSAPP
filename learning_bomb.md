# 反编译
objdump -d bomb > bomb.asm

# gdb 使用
指令	全称	描述
r	run	开始执行程序，直到下一个断点或程序结束
q	quit	退出 GDB 调试器
ni	nexti	执行下一条指令，但不进入函数内部
si	stepi	执行当前指令，如果是函数调用则进入函数
b	break	在指定位置设置断点
c	cont	从当前位置继续执行程序，直到下一个断点或程序结束
p	print	打印变量的值
x		打印内存中的值
j	jump	跳转到程序指定位置
disas		反汇编当前函数或指定的代码区域
layout asm		显示汇编代码视图
layout regs		显示当前的寄存器状态和它们的值

我一般启动 gdb bomb 后，都会首先使用 layout asm、layout regs 开启视图，方便分析。

关闭 layout 的方式为，按下 Ctrl + x，然后再按下 a。

# gdb 有一个很实用的功能，就是我们可以使用 .gdbinit 文件来设置 gdb 进入时的一些默认配置

p $rax  # 打印寄存器 rax 的值
p $rsp  # 打印栈指针的值
p/x $rsp  # 打印栈指针的值，以十六进制显示
p/d $rsp  # 打印栈指针的值，以十进制显示

这些命令在 / 后面的后缀（如 2x、2d、s、g、20c）指定了查看内存的方式和数量。具体来说：

第一个数字（如 2、20）指定要查看的单位数量。

第二个字母（如 x、d、s、g、c）指定单位类型和显示格式，其中：

c / d / x 分别代表以字符 / 十进制 / 十六进制格式显示内存内容。

s 代表以字符串格式显示内存内容。

b / h / w / g 分别代表以 1 / 2 / 4 / 8 字节为单位（unit）显示内存内容。

当使用 x/b、x/h、x/w、x/g 时，unit 会保留对应改变，直到你再次使用这些命令

# Bomb Lab 的程序是“无状态”的。它不会把你的进度保存到文件、数据库或任何持久化存储中

# phase_1

第 1 步：设断点并运行
(gdb) break phase_1
(gdb) run

第 2 步：反汇编看关键指令
(gdb) disas phase_1

分析
x/s 0x402400

x：examine，检查内存
/s：格式修饰符，表示 *以 C 字符串（char ）方式显示
0x402400：要查看的内存地址

# 在进入 Phase 2 之前下断点：
设置断点是使用函数名来限定的
(gdb) break phase_2
(gdb) run

c //继续

# mov $0xcf,%eax     ; 207
其中$0xcf表示十六进制数字，就是207

# phase_3
0 207 或 1 707 等

# phase_4
0<= n1 <= 14 n2=0
eg:
1 0

# phase_5

输入一个长度为 6 的字符串​
对每一个字符：
取低 4 位（& 0xf）
用这个值当索引，去查一个“神秘字符数组”
得到 6 个新字符
这 6 个字符必须等于某个目标字符串

# phase_6
输入 6 个 1~6 的编号，不能重复；
用这些编号去链表里取节点；
重排链表后，要求节点里的 value 是严格降序的。
phase_6 要求的是 7 - x
